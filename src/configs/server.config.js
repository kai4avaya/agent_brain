const config = {
    secret: 'password', // Replace this with your actual secret key
    tokenExpiry: '200h', // Token expiration time
    username: 'username', // Default username
    port: 3000, // Server port
  };

  export default config;