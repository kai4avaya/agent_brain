// import { embed } from '../src/embeddings/embed.js';

// describe('embed', () => {
//   it('should return an object', async () => {
//     const text = 'test text';
//     const embeddings = await embed(text);
//     expect(typeof embeddings).toBe('object');
//   });
// });